import React from "react";
import { Navbar } from "react-bootstrap";

const Header = () => {
	return (
		<Navbar className="header d-flex justify-content-center" expand="lg">
			<Navbar.Brand>STAR WARS</Navbar.Brand>
		</Navbar>
	);
};

export default Header;
